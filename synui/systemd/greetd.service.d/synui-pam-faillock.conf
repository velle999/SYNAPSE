# Converge the faillock stack whenever greetd starts — see
# synui-pam-faillock.service. A drop-in rather than an [Install] symlink, for
# the same reason as synui-login-fprint.conf beside it: nothing to enable.
[Unit]
Wants=synui-pam-faillock.service
After=synui-pam-faillock.service
