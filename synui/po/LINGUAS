# The languages synui has catalogs for, one per line. Same set as
# syn-install's LOCALE_ROWS and synsh's — a system installed in Polish whose
# desktop then answered in English would be the "asked and then ignored"
# failure that whole table exists to prevent.
de
fr
es
pt
it
nl
pl
ru
ja
zh
ko
# hi and ar. They were listed here from 572 and did not RENDER correctly until
# 581: src/text.c drew through cairo's toy API with a fallback face chooser and
# no shaper, so Arabic came out as isolated letter forms in reverse order and
# Devanagari with its matras unreordered. The catalogs were never the problem
# and were never redone. HarfBuzz and FriBidi land in text.c at 581, and
# tests/text_shape_test.c pins the three properties that say it worked: letters
# join, conjuncts form, and reversing a string's logical order draws the same
# line. The other eleven never needed any of it — Latin and Cyrillic need no
# shaping and CJK needs only face fallback.
hi
ar
