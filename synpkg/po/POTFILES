# The QML files tools/qml-xgettext.py reads, one per line.
#
# ⚠ A FILE MISSING FROM HERE IS SILENTLY ENGLISH FOREVER — its I18n.tr() calls
# still compile and still look up at runtime, so nothing warns; the strings just
# never reach a catalog. tests/i18n_test.sh fails on a .qml under data/ that
# calls I18n.tr() and is not listed.
synpkg.qml
