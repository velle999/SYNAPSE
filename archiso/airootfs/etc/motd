
  SynapseOS 0.2.2 — Where the kernel thinks.

  Services:  synapd  synnet  synguard  synapse_kmod
  Shell:     synsh    (type naturally or use shell commands)
  Compositor: synui  (WLR_BACKENDS=drm synui)

  The AI model ships on this media — synapd runs here, on CPU, so it is
  slow but real. `syn status` shows the model and the daemon.

  To install to disk:  syn-install

