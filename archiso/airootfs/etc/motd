
  SynapseOS 0.2.9.5 — Where the kernel thinks.

  Services:  synapd  synnet  synguard  synapse_kmod
  Shell:     synsh    (type naturally or use shell commands)
  Compositor: synui  (WLR_BACKENDS=drm synui)

  No AI model ships on this media, so synapd here has nothing to load —
  the live session is the desktop, not the AI. The installer asks which
  model to download onto the machine it installs.

  To install to disk:  syn-install

