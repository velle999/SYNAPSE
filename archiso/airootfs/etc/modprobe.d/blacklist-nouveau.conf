# nouveau must never grab an NVIDIA card the nvidia driver can run —
# synui has no working EGL/Vulkan on nouveau. For cards nvidia-open does
# not support (pre-Turing), synui-gfx-env modprobes nouveau explicitly
# and falls back to software rendering.
blacklist nouveau
