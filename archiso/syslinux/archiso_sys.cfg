INCLUDE archiso_head.cfg

DEFAULT arch
TIMEOUT 150

INCLUDE archiso_sys-linux.cfg

# The language submenu, above the tail so it sits with the boot entries rather
# than with reboot/poweroff. It is a submenu and not fifteen top-level rows
# because the main menu is what somebody in a hurry reads.
INCLUDE archiso_lang.cfg

INCLUDE archiso_tail.cfg
